config:
  # insert custom config here



  # playwright engine configuration
  engines:
    playwright:
      launchOptions:
        headless: true
  # constant path
  processor: "./playwright-scenario.js"
  # required but unused target
  target: "http://localhost"
scenarios:
  - engine: playwright
    testFunction: "usageScenario"
